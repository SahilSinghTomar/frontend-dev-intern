const loadingStyle = {
  display: "flex",
  fontSize: "2rem",
  alignItems: "center",
  justifyContent: "center",
};

const Loading = () => {
  return <div style={loadingStyle}>LOADING...</div>;
};

export default Loading;
